<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TaiExam.mapper.RolePermissionMapper">

    <!--  新增权限  -->
    <insert id="insertRolePermission" parameterType="RolePermission" useGeneratedKeys="true" keyProperty="id">
        insert into role_permission
            (role_id, permission_id, create_time)
        VALUES
            (#{roleId}, #{permissionId}, Now())
    </insert>

    <!-- 删除权限 -->
    <delete id="deleteRolePermissionById" parameterType="int">
        DELETE FROM permission WHERE id = #{id}
    </delete>

    <!-- 删除权限 -->
    <delete id="deleteRolePermissionByRoleId" parameterType="int">
        DELETE FROM role_permission WHERE role_id = #{roleId}
    </delete>

    <!-- 通过角色id查找权限 -->
    <select id="getPermissionsByRoleId" parameterType="int" resultType="Permission">
        select p.*
        from permission p
                 inner join role_permission rp on p.id = rp.permission_id
        where rp.role_id = #{roleId}
    </select>

    <!--  通过role 查询权限id列表   -->
    <select id="getPermissionIdsByRoleId" parameterType="int" resultType="int">
        select permission_id from permission where role_id = #{roleId}
    </select>

    <!-- 根据roleId获取权限码列表 -->
    <select id="getPermissionCodesByRoleId" parameterType="int" resultType="String">
        select p.permission_code
        from permission p
                 inner join role_permission rp on p.id = rp.permission_id
        where rp.role_id = #{roleId}
    </select>

    <!--  通过权限id 查询角色id列表   -->
    <select id="getRoleIdsByPermissionId" parameterType="String" resultType="String">
        select role_id from role_permission where permission_id = #{permissionId}
    </select>

</mapper>